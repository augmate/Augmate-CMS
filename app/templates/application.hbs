{{bs-growl-notifications}}
{{bs-notifications style="z-index: 1000; position: fixed; width: 50%; left: 0; right: 0; margin-left: auto; margin-right: auto; margin-top: 50px;"}}

{{#if isLoggedIn}}
    <div class="nav-tabs">
        <a class="tab active" href="#">Apps</a>
        <a class="tab" href="#">Contact</a>
        <a class="tab" href="#">Log Out</a>
    </div>
    <div class="container">
        {{outlet}}
    </div>
{{else}}
    <div class="container">
        {{outlet}}
    </div>
{{/if}}
