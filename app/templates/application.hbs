{{bs-growl-notifications}}
{{bs-notifications style="z-index: 1000; position: fixed; width: 50%; left: 0; right: 0; margin-left: auto; margin-right: auto; margin-top: 50px;"}}

{{#if isLoggedIn}}
    <div class="nav-tabs">
        {{#linkTo "applist" class="tab"}}Apps{{/linkTo}}
        {{#linkTo "contact_us" class="tab"}}Contact{{/linkTo}}
        {{#linkTo "login" class="tab"}}Log Out{{/linkTo}}
    </div>
    <div class="container">
        {{outlet}}
    </div>
{{else}}
    <div class="container">
        {{outlet}}
    </div>
{{/if}}
